<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/product.css">
    <title><%= product.title %></title>
</head>
<body class="productPage">
    <%- include("./components/Header.ejs") %>
    <section class="preductSection">
        <div class="up">
            <div class="left">
                <% if(product && product.image){ %>
                    <img src="/<%= product.image %>" alt="image1">
                <% } else{ %>
                    <img src="https://upload.wikimedia.org/wikipedia/commons/1/14/No_Image_Available.jpg" alt="no image found">
                <% } %>
            </div>
            <div class="right">
                <h1 class="title"><%= product.title %></h1>
                <p class="price">$<%= product.price %></p>
                <form action="/add-product-cart/<%= product._id %>" method="POST">
                    <button class="addToCart">Add to Cart</button>
                </form>
            </div>
        </div>
        <div class="desc">
            <%= product.desc %>
        </div>
    </section>
</body>
</html>